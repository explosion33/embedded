name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  host-tests:
    uses: ./.github/workflows/bazel_test_template.yml
    with:
      test_target: '//...'
