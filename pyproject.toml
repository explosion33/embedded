[project]
name = "embedded"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

# Adds clang-format to the uv.lock allowing bazel rules python to read and use
# these packages.
[dependency-groups]
dev = [
    "clang-format==23.1.1",
]
